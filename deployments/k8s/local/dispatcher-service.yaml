apiVersion: v1
kind: Service
metadata:
  name: zenith-dispatcher
  labels:
    app: zenith-dispatcher
spec:
  type: ClusterIP
  selector:
    app: zenith-dispatcher
  ports:
  - name: http
    port: 8081
    targetPort: 8081
    protocol: TCP
